# Car AI runtime dependencies.
#
# ollama >= 0.5 is REQUIRED, not merely preferred: the tool-result message
# field `tool_name` does not exist on the Message model in 0.4.x. On 0.4.x it
# is silently dropped, so a turn containing two tool calls gives the model two
# anonymous results it cannot match to its own calls.
ollama>=0.5.0

# Live OBD-II reads over an ELM327 adapter.
obd>=0.7.2

# Local dashboard + chat UI.
flask>=3.0.0
